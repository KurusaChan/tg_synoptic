Зараз у місті <b>{{ weather.city }}</b>
<code>
• {{ weather.date }} {{ weather.icon }} {{ weather.temp }}° {{ weather.desc }}
    💨швидкість вітру: {{ weather.wind_speed }} м/с
    💨тиск: {{ weather.pressure }} мм
    ☁️хмарність: {{ weather.clouds }}%
    ☁вологість: {{ weather.humidity }}%
</code>
☀схід: <code>{{ weather.sunrise }}</code> захід: <code>{{ weather.sunset }}</code>

<i>{{ weather.detail }}</i>
